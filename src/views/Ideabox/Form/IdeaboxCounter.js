import React from "react";

const IdeaboxCounter = ({ value }) => {
  return (
    <div>
      <strong>{value}</strong> Idea sheet has been approved
    </div>
  );
};

export default IdeaboxCounter;
